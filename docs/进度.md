# 进度表

## 已完成
- [x] 拉取空仓库 `MageGojo/QuantumultX`
- [x] 确认图标方案（AI=lobehub，功能 / 地区=Koolson，均走 jsDelivr）并验证 URL 可用
- [x] 验证 blackmatrix7 分流规则源可用（13 条全部 200）
- [x] 编写设计文档 `docs/设计.md`
- [x] 编写主配置 `MageGojo.conf`（订阅 / 策略组 / 分流 / 地区筛选）
- [x] 编写 `README.md`
- [x] 提交并推送到 GitHub
- [x] 修复语法：`geo_location_check`→`geo_location_checker`、DNS 改用 `doh-server`
- [x] 补齐全部 12 个标准 section（缺失的 5 个以空模块占位）
- [x] 配置仅经 GitHub 分发：移除 README 中指向本配置的 jsDelivr 链接

## 待办 / 下一步
- 用户填入真实机场订阅链接后，验证节点筛选与图标加载效果。
- 如某地区无节点，删除对应地区组（`[policy]` 中的 `url-latency-benchmark=地区`）。
- 可选：增加去广告 rewrite、扩展更多流媒体规则。

## 关键决策
- 单一图标库无法覆盖「AI 专属 + 地区 / 广告 / 兜底」全场景，故 AI 区用 lobehub、其余用 Koolson，各自风格统一。
- 节点筛选用 QuantumultX 的 `server-tag-regex` 在策略组层面完成，免维护节点名单。
- 图标与规则统一走 `cdn.jsdelivr.net`，兼顾国内可达性与稳定性。
